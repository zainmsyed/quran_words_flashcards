import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.quranicflashcards',
  appName: 'Quranic Flashcards',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
