# Code Review 2026-04-10T12:30:59Z

**Status:** complete
**Created:** 2026-04-10T12:30:59Z
**Completed:** 2026-04-10
**Scope:** story
**Story:** story-001
**Focus:** story-001 completion review
**Trigger:** complete-story

---

## Goal
[Inspect the requested scope for bugs, regressions, missing tests, dead code, simplification opportunities, scope drift, and workflow violations.]

## Checklist
- [x] Inspect the relevant diff and touched files
- [x] Check for bugs, regressions, and edge cases
- [x] Check tests and verification gaps
- [x] Check for dead code, duplication, and simplification opportunities
- [x] Capture reusable rule candidates where warranted
- [x] Write the completion summary and mark the review complete

---

## Findings
### Finding 1
- Severity: high
- Category: bug
- Summary: `pb_migrations/002_create_card_progress.js` declares an invalid 14-character PocketBase collection id, which is likely to fail migration validation on startup.
- Evidence: The file sets `id: "qfc2cardprog01"`; that id is 14 characters long, while the matching auth collection id `qfc2usersauth01` is 15 characters. The migration also references this id from the `user` relation, so the invalid id affects the collection creation path directly.
- Recommendation: Replace the custom id with a valid 15-character collection id or let PocketBase generate the id, then update the relation reference if needed.
- Rule candidate: —

---

### Finding 2
- Severity: medium
- Category: test-gap
- Summary: Story 001 is documented as complete in the repo, but the PocketBase migrations and access rules have not been validated against a live PocketBase binary in a repeatable smoke test.
- Evidence: `.context/stories/story-001.md` explicitly notes that runtime validation is still pending, and the repository does not include a smoke-test script or CI check that starts PocketBase, runs migrations, and exercises the auth/card_progress endpoints.
- Recommendation: Run a live PocketBase startup against the target version and add a repeatable smoke test or CI check that verifies migration success, admin bootstrap, and basic auth/card_progress access.
- Rule candidate: —

## Recommended Fixes
- [x] high — fix the invalid PocketBase collection id in `pb_migrations/002_create_card_progress.js`
- [x] medium — add a repeatable PocketBase smoke test that verifies migrations, bootstrap, and access rules

---

## Other Fixes
[Add any additional follow-up tasks here using `- [ ] severity — action` or simple `- [ ] description`. Mark items [x] only after the work is complete.]

---

## Completion Summary
Review follow-up complete for story-001.

Remediation applied:
1. Fixed the invalid PocketBase collection id in `pb_migrations/002_create_card_progress.js` by replacing it with a valid 15-character id and updating the rollback lookup to match.
2. Added `scripts/pocketbase-smoke-test.mjs` plus a `npm run smoke:pocketbase` entry so the backend foundation can be smoke-tested repeatably.
3. Extended the PocketBase setup docs to describe the smoke test and the verification sequence.

Verification completed:
- `node --check scripts/pocketbase-smoke-test.mjs`
- `node --check pb_migrations/002_create_card_progress.js`
- `npm test`

Residual note:
- The smoke test is now in place and ready to run against a real PocketBase binary; in this environment I verified syntax and repo tests, but I did not execute a live PocketBase server.
