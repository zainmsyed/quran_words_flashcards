# Code Review 2026-04-22T11:12:52Z

**Status:** complete  
**Created:** 2026-04-22T11:12:52Z  
**Completed:** 2026-04-22  
**Scope:** whole-codebase  
**Story:** —  
**Focus:** whole codebase review  
**Trigger:** manual

---

## Goal
Inspect the whole codebase for bugs, regressions, missing tests, dead code, simplification opportunities, scope drift, and workflow violations.

## Checklist
- [x] Inspect the relevant diff and touched files
- [x] Check for bugs, regressions, and edge cases
- [x] Check tests and verification gaps
- [x] Check for dead code, duplication, and simplification opportunities
- [x] Capture reusable rule candidates where warranted
- [x] Write the completion summary and mark the review complete

---

## Findings
### Finding 1
- Severity: low
- Category: workflow
- Summary: A timestamped backup file is tracked in the repository even though it is not part of any runtime, review, or planning workflow.
- Evidence: `.context/reviews/mispronunciations.md.bak-20260422-025019` is present in `git ls-files`, and `rg -n "mispronunciations\.md\.bak-20260422-025019|mispronunciations\.md\.bak" -S .` returns no references anywhere else in the codebase.
- Recommendation: Remove the committed backup file from source control and keep future one-off backups outside the tracked tree (or add an ignore pattern if local backups are still useful).
- Rule candidate: Do not commit timestamped backup artifacts into tracked project directories; keep them outside the repo or gitignored.

### Finding 2
- Severity: low
- Category: dead-code
- Summary: `src/ui/StudySession.svelte` still contains component-scoped CSS for the removed session quota badges, so every production build now emits avoidable unused-selector warnings.
- Evidence: `npm run build` succeeds but warns that `.session-quota`, `.quota-badges`, `.badge`, `.badge-count`, `.badge-label`, `.badge-new`, and `.badge-review` are unused; the stale selectors are still present in `src/ui/StudySession.svelte:659-702` even though the badge markup was removed.
- Recommendation: Delete the unused selectors (or restore the matching markup) so the component stylesheet matches the rendered DOM and the build stays warning-free.
- Rule candidate: When removing component markup, remove its component-scoped selectors in the same change and verify with a production build.

---

## Recommended Fixes
- [x] low — Remove `.context/reviews/mispronunciations.md.bak-20260422-025019` from source control (and keep future backups untracked).
  - Implemented: the file was untracked (`git rm --cached`) and moved to `.artifacts/backups/mispronunciations.md.bak-20260422-025019`; local exclude added so future `.bak` files in `.context/reviews` are ignored.
- [x] low — Remove the orphaned session quota badge selectors from `src/ui/StudySession.svelte` so `npm run build` is warning-free.
  - Implemented: removed the unused component-scoped selectors. `npm run build` now completes without unused-selector warnings and `npm run test` passes.

---

## Other Fixes
- No additional follow-up items beyond the findings above.

---

## Completion Summary
Reviewed the current whole-codebase state, including helper scripts, review artifacts, UI, and the build/test verification path. `npm run test` passes, and `npm run build` succeeds, but the build currently emits unused-selector warnings from `StudySession.svelte`. No functional regressions stood out in this pass, but two low-severity cleanup/workflow issues remain: a committed backup artifact under `.context/reviews/` and dead component-scoped CSS left behind after the session badge removal. These do not block normal app behavior, but they should be cleaned up before treating the codebase as fully tidy.
