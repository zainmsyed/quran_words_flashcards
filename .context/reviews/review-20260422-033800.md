# Code Review 2026-04-22T03:38:00Z

**Status:** complete  
**Created:** 2026-04-22T03:38:00Z  
**Completed:** 2026-04-22  
**Scope:** story  
**Story:** story-020  
**Focus:** story-020 completion review  
**Trigger:** complete-story

---

## Goal
Inspect story-020 and its direct integration points for bugs, regressions, missing tests, dead code, simplification opportunities, scope drift, and workflow violations.

## Checklist
- [x] Inspect the relevant diff and touched files
- [x] Check for bugs, regressions, and edge cases
- [x] Check tests and verification gaps
- [x] Check for dead code, duplication, and simplification opportunities
- [x] Capture reusable rule candidates where warranted
- [x] Write the completion summary and mark the review complete

## Findings
### Finding 1
- Severity: medium
- Category: dead-code
- Summary: `scripts/regenerate_review_gtts.py` is an extra regeneration path that is not wired into package scripts, tests, or docs, so it duplicates the reviewed gTTS flow without being exercised.
- Evidence: `rg -n "regenerate_review_gtts" -S .` only matches the script itself; `package.json` has no corresponding npm script; `scripts/run-tests.mjs` does not cover it.
- Recommendation: Remove the script or wire it into the documented workflow and add test coverage so it does not drift from the supported regeneration path.
- Rule candidate: When you add a helper script for a finished workflow, wire it into the documented/package entry points and tests, or delete it before merge.

### Finding 2
- Severity: medium
- Category: test-gap
- Summary: The new w59/w82 playback suppression in `src/ui/components/Card.svelte` is only covered by helper-level TTS tests, so a UI regression could reintroduce the audio button without failing the suite.
- Evidence: `scripts/run-tests.mjs` adds unit coverage for `canPronounceWord`/`speak`, but there is no component test that mounts `Card.svelte` and asserts the `.audio-btn` is hidden for blocked ids.
- Recommendation: Add a component-level test for `Card.svelte` (or the session screen) that renders blocked words and verifies the pronunciation button does not appear.
- Rule candidate: When UI availability depends on helper state, add a render-level test for the control, not only helper tests.

## Recommended Fixes
- [x] medium — Remove or wire `scripts/regenerate_review_gtts.py` so the review workflow has a single documented regeneration path.
  - Implemented: removed the unreferenced helper script after verifying it was not wired into package scripts, docs, or tests.
- [x] medium — Add a Card.svelte component test that verifies blocked ids like w59/w82 do not render a pronunciation button.
  - Implemented: added a Svelte component integration test in `scripts/run-tests.mjs` that mounts `Card.svelte`, verifies a normal word still renders the button, and confirms blocked ids (`w59`, `w82`) do not.

## Other Fixes
- No additional follow-up items beyond the findings above.

## Completion Summary
Reviewed story-020 and its direct integration points. The two remediation items identified in this review are now complete: the stray `scripts/regenerate_review_gtts.py` helper was removed, and a Card.svelte integration test now covers blocked pronunciation controls for `w59` and `w82`. Verified with `npm run test`; no remaining blockers are open in this review.
